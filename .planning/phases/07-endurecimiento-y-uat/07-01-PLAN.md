---
phase: 07-endurecimiento-y-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/vitest.config.js
  - backend/src/routes/routing.test.js
autonomous: true
requirements: [HARDEN-01]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "El backend tiene Vitest configurado (script test + vitest.config.js) y corre sin pegarle a ArcGIS/OSRM reales."
    - "Hay pruebas de contrato reales de POST /api/rutas/resolver para éxito ArcGIS, fallos de red/HTTP/cuerpo inválido con respaldo OSRM, y 502 cuando ambos fallan."
    - "startTime=now y timeout de ArcGIS quedan como it.todo explícitos porque TRAFFIC-01 y el timeout no existen en producción; un test real documenta que hoy el body NO incluye startTime."
  artifacts:
    - backend/vitest.config.js
    - backend/src/routes/routing.test.js
  key_links:
    - "routing.js POST /resolver -> arcgisRouting.solve (sin startTime) -> osrmRouting respaldo"
  prohibitions:
    - "No implementar TravelTime/startTime=now ni timeouts solo para poner un test en verde."
    - "No registrar API keys reales ni coordenadas de dispositivos personales."
---

<objective>
Configurar Vitest en el backend y dejar pruebas de contrato reales contra el proxy de rutas existente, con placeholders explícitos para tráfico/timeout aún no implementados.
</objective>

<tasks>
<task type="auto">
  <name>Vitest backend + contrato POST /api/rutas/resolver</name>
  <files>backend/package.json, backend/vitest.config.js, backend/src/routes/routing.test.js</files>
  <action>
    Conservar la config parcial ya iniciada. Completar tests de contrato con fetch mockeado. Documentar ausencia de startTime en el body actual. it.todo para TRAFFIC-01 y timeout.
  </action>
  <verify>
    <command>npm test</command>
    <cwd>backend</cwd>
  </verify>
</task>
</tasks>
