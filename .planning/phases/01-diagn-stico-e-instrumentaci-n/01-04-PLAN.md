---
phase: 01-diagn-stico-e-instrumentaci-n
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - frontend/src/componentes/detalle/InteractiveMap.jsx
  - frontend/src/componentes/detalle/InteractiveMap.test.jsx
autonomous: true
requirements: [DIAG-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Los 6 tramos de latencia pedidos por ROADMAP.md Fase 1 quedan completamente instrumentados y medibles de extremo a extremo tras este plan (tramos 4 y 5 desde Plan 01-01, arranques de 1/2/3 desde Plan 01-03, extremos finales de 1/2/3/6 en este plan)."
    - "El tramo GPS->marcador mide desde la fijacion GPS aceptada hasta el primer frame de animacion visual del marcador (usePosicionAnimada), no hasta el final de la animacion de 600ms."
    - "El tramo GPS->camara mide desde la fijacion GPS aceptada hasta el instante en que se invoca map.easeTo(...), documentado como excluyendo la duracion propia de la animacion (250ms)."
    - "El tramo respuesta->ruta renderizada mide desde que useNavegacion recibe la respuesta de ArcGIS hasta que InteractiveMap confirma via efecto que los datos de la ruta llegaron al componente."
  artifacts: []
  key_links:
    - "InteractiveMap.jsx usePosicionAnimada() (primer frame de animacion) -> medir(TRAMOS.GPS_MARCADOR, MARCAS.GPS_ACEPTADO, MARCAS.MARCADOR_RENDER)"
    - "InteractiveMap.jsx efecto de camara (map.easeTo) -> medir(TRAMOS.GPS_CAMARA, MARCAS.GPS_ACEPTADO, MARCAS.CAMARA_ACTUALIZADA)"
    - "InteractiveMap.jsx nuevo efecto sobre orientacion.heading -> medir(TRAMOS.ORIENTACION_FLECHA, MARCAS.ORIENTACION_CAMBIO, MARCAS.FLECHA_RENDER)"
    - "InteractiveMap.jsx nuevo efecto sobre tramos.restante -> medir(TRAMOS.RESPUESTA_RUTA_RENDERIZADA, MARCAS.RESPUESTA_RECIBIDA, MARCAS.RUTA_RENDERIZADA)"
  prohibitions:
    - "La instrumentacion no debe registrar coordenadas GPS, rumbo o geometria de ruta en ningun mark/measure -- solo nombres de tramo fijos."
    - "Esta fase no debe modificar el comportamiento de camara, flecha o renderizado de ruta -- solo observarlo. Ningun valor de PITCH_NAVEGACION, ZOOM_NAVEGACION, duration de easeTo/fitBounds ni la logica de enSeguimiento/siguiendo cambia en este plan."
---

<objective>
Completar la instrumentacion de los 6 tramos de latencia añadiendo los extremos finales de los tramos 1 (GPS->marcador), 2 (orientacion->flecha), 3 (GPS->camara) y 6 (respuesta->ruta renderizada) en InteractiveMap.jsx, emparejando contra las marcas de arranque ya emitidas en useGeolocation.js/useOrientacion.js (Plan 01-03) y useNavegacion.js (Plan 01-01). Al terminar este plan, los 6 tramos pedidos por ROADMAP.md Fase 1 Success Criteria #1 quedan completamente medibles en un dispositivo real.

Purpose: cerrar el diagnostico de latencia sin tocar el comportamiento visual real de la camara, la flecha o el trazado de ruta -- Fase 1 observa, no corrige (D-01..D-04, 01-CONTEXT.md <domain>).
Output: InteractiveMap.jsx con 4 puntos de medicion nuevos; InteractiveMap.test.jsx extendido para cubrirlos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-diagn-stico-e-instrumentaci-n/01-CONTEXT.md
@frontend/src/componentes/detalle/InteractiveMap.jsx
@frontend/src/componentes/detalle/InteractiveMap.test.jsx
@frontend/src/utilidades/diagnosticoLatencias.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extremos finales de los tramos 1 (marcador), 2 (flecha) y 3 (camara)</name>
  <files>frontend/src/componentes/detalle/InteractiveMap.jsx, frontend/src/componentes/detalle/InteractiveMap.test.jsx</files>
  <read_first>
    frontend/src/componentes/detalle/InteractiveMap.jsx funcion usePosicionAnimada (~linea 73-113, en particular la funcion interna `paso` y la variable `inicio` que arranca en null en cada nuevo ciclo de animacion); el efecto de camara (~linea 286-298, `if (!mapListo || !enSeguimiento || !siguiendo || !userPosition) return; ... map.easeTo({...})`); el uso de orientacion.heading en el calculo de rotacionFlecha (~linea 354-360, dentro del cuerpo de render, NO dentro de un efecto -- este task añade un efecto nuevo, no modifica ese calculo); frontend/src/componentes/detalle/InteractiveMap.test.jsx (patron de mock: vi.mock('react-map-gl/maplibre', ...) con el objeto `map` espiado, vi.mock('../../hooks/useOrientacion', ...) ya mockeado con heading fijo -- este task necesitara sobreescribir ese mock en tests especificos para variar heading entre renders).
  </read_first>
  <behavior>
    - En el primer frame de animacion de usePosicionAnimada (cuando `inicio` se asigna por primera vez dentro de `paso`, es decir cuando origen existia y el RAF arranca), se dispara marcar(MARCAS.MARCADOR_RENDER) y medir(TRAMOS.GPS_MARCADOR, MARCAS.GPS_ACEPTADO, MARCAS.MARCADOR_RENDER) una sola vez por ciclo de animacion, no en cada frame de RAF.
    - Un nuevo efecto de React, separado del render body, disparado por cambios en orientacion.heading, marca MARCAS.FLECHA_RENDER y mide TRAMOS.ORIENTACION_FLECHA cuando orientacion.heading no es null.
    - Dentro del efecto de camara existente, justo donde se invoca map.easeTo(...), se dispara marcar(MARCAS.CAMARA_ACTUALIZADA) y medir(TRAMOS.GPS_CAMARA, MARCAS.GPS_ACEPTADO, MARCAS.CAMARA_ACTUALIZADA).
  </behavior>
  <action>
    Importar { marcar, medir, MARCAS, TRAMOS } desde '../../utilidades/diagnosticoLatencias' en InteractiveMap.jsx. Dentro de usePosicionAnimada, en la funcion `paso`, en la rama `if (inicio == null) { inicio = t; ... }` (el primer frame del ciclo de animacion), añadir marcar(MARCAS.MARCADOR_RENDER) seguido de medir(TRAMOS.GPS_MARCADOR, MARCAS.GPS_ACEPTADO, MARCAS.MARCADOR_RENDER). Añadir un nuevo useEffect en el cuerpo de InteractiveMap (junto a los efectos de camara existentes), con dependencia [orientacion.heading], que si orientacion.heading no es null llame marcar(MARCAS.FLECHA_RENDER) y medir(TRAMOS.ORIENTACION_FLECHA, MARCAS.ORIENTACION_CAMBIO, MARCAS.FLECHA_RENDER). Dentro del efecto de camara existente (el que llama map.easeTo dentro de `if (!mapListo || !enSeguimiento || !siguiendo || !userPosition) return;`), justo antes o despues de la llamada a map.easeTo({...}) (sin alterar sus parametros: center, bearing, pitch, zoom, duration deben quedar exactamente iguales), añadir marcar(MARCAS.CAMARA_ACTUALIZADA) y medir(TRAMOS.GPS_CAMARA, MARCAS.GPS_ACEPTADO, MARCAS.CAMARA_ACTUALIZADA).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/componentes/detalle/InteractiveMap.test.jsx</automated>
  </verify>
  <done>Los tres extremos finales disparan measure con los nombres diag:gps-marcador, diag:orientacion-flecha y diag:gps-camara, verificado por test con performance.mark/measure espiados, sin cambiar ningun parametro real de map.easeTo (center/bearing/pitch/zoom/duration siguen exactos, cubierto por los tests preexistentes de camara que no deben romperse).</done>
  <acceptance_criteria>
    - Los tests de camara preexistentes en InteractiveMap.test.jsx ('owns a trusted live camera update...', 'keeps the current bearing...', 'does not recenter...', 'returns explicitly to north-up...') siguen en verde sin modificar sus expect(map.easeTo).toHaveBeenLastCalledWith(...) -- confirma que este plan no altero el comportamiento real de camara.
    - Un test nuevo con performance.mark/measure espiados confirma que al montar InteractiveMap con navegando=true y una posicion valida se registra una measure 'diag:gps-camara'.
    - Un test nuevo que sobreescribe el mock de useOrientacion para devolver heading no nulo confirma una measure 'diag:orientacion-flecha'.
    - Un test nuevo confirma que un ciclo de animacion de usePosicionAnimada (posicion cambia entre renders) registra una measure 'diag:gps-marcador'.
    - `npx vitest run src/componentes/detalle/InteractiveMap.test.jsx` termina en verde.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extremo final del tramo 6 (respuesta->ruta renderizada)</name>
  <files>frontend/src/componentes/detalle/InteractiveMap.jsx, frontend/src/componentes/detalle/InteractiveMap.test.jsx</files>
  <read_first>
    frontend/src/componentes/detalle/InteractiveMap.jsx: la variable `mostrarTrayecto` (~linea 255) y el JSX que renderiza los Source/Layer 'ruta-recorrida'/'ruta-restante' a partir de `tramos?.recorrido`/`tramos?.restante` (~linea 435-454); el objeto `tramos` viene del NavegacionContext (mismo shape que useNavegacion.js expone: { recorrido: [], restante: [] }).
  </read_first>
  <behavior>
    - Un nuevo efecto de React disparado cuando mostrarTrayecto es true y tramos.restante tiene al menos un punto, marca MARCAS.RUTA_RENDERIZADA y mide TRAMOS.RESPUESTA_RUTA_RENDERIZADA una vez por cada nuevo conjunto de puntos de ruta (no en cada render sin cambios reales en la ruta).
  </behavior>
  <action>
    Añadir un nuevo useEffect en InteractiveMap.jsx, junto a los efectos existentes, con dependencia [tramos] (o [tramos?.restante] si se prefiere una comparacion mas fina), que si mostrarTrayecto es true y tramos?.restante?.length es mayor que 0, llame marcar(MARCAS.RUTA_RENDERIZADA) seguido de medir(TRAMOS.RESPUESTA_RUTA_RENDERIZADA, MARCAS.RESPUESTA_RECIBIDA, MARCAS.RUTA_RENDERIZADA). No alterar el JSX de renderizado de Source/Layer existente ni el calculo de mostrarTrayecto/lineaGeoJSON.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/componentes/detalle/InteractiveMap.test.jsx</automated>
  </verify>
  <done>El sexto y ultimo tramo (respuesta->ruta renderizada) dispara measure 'diag:respuesta-ruta-renderizada' cuando el componente recibe una ruta con puntos y mostrarTrayecto es true, verificado por test.</done>
  <acceptance_criteria>
    - Un test que renderiza InteractiveMap con navegando=true, ruta con puntos y tramos.restante no vacio confirma una measure 'diag:respuesta-ruta-renderizada'.
    - Un test que renderiza InteractiveMap con showRoute=false (mapa informativo, mostrarTrayecto=false) confirma CERO llamadas a performance.mark('ruta:renderizada').
    - `npx vitest run src/componentes/detalle/InteractiveMap.test.jsx` termina en verde con la suite completa del archivo.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InteractiveMap.jsx render/effect boundary | Where GPS/orientation/route state crosses from hooks into the rendered map -- instrumentation observes this boundary without altering it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | InteractiveMap.jsx marcar()/medir() call sites | high | mitigate | All four new call sites pass only static MARCAS/TRAMOS constants -- never userPosition.lat/lng, orientacion.heading value, or ruta.puntos -- verified by test asserting mark/measure name arguments |
| T-01-03 | Denial of Service (resource growth) | Browser Performance timeline buffer during a real GPS-tick-per-second navigation session | low | mitigate | resumen() (Plan 01-01) already clears marks/measures after aggregating; this plan adds no additional unbounded accumulation source since usePosicionAnimada's mark only fires once per animation cycle (guarded by `inicio == null`), not once per RAF frame |
</threat_model>

<verification>
`cd frontend && npx vitest run src/componentes/detalle/InteractiveMap.test.jsx` -- full suite green, including the 6 preexisting camera-lifecycle tests (unchanged behavior) and the new instrumentation tests for tramos 1, 2, 3, and 6.
</verification>

<success_criteria>
- All 6 latency tramos required by ROADMAP.md Phase 1 Success Criteria #1 are wired end-to-end and produce real performance.measure entries.
- No change to camera parameters (center/bearing/pitch/zoom/duration), arrow rotation logic, or route rendering.
</success_criteria>

<output>
Create `.planning/phases/01-diagn-stico-e-instrumentaci-n/01-04-SUMMARY.md` when done
</output>
