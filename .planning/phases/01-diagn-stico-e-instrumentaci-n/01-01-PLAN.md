---
phase: 01-diagn-stico-e-instrumentaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/utilidades/diagnosticoLatencias.js
  - frontend/src/utilidades/diagnosticoLatencias.test.js
  - frontend/src/hooks/useNavegacion.js
  - frontend/src/hooks/useNavegacion.test.js
autonomous: true
requirements: [DIAG-01]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Existe un mecanismo de instrumentación dev-only (performance.mark/performance.measure) que nunca registra coordenadas, latitud/longitud, rumbo ni datos de ruta — solo nombres de tramo y duraciones numéricas."
    - "El mecanismo está inactivo (no llama a performance.mark/measure) cuando import.meta.env.DEV es false, verificado por test, no por inspección visual."
    - "El tramo 'solicitud→respuesta ArcGIS' (5 de 6) produce una medición real end-to-end cuando useNavegacion() resuelve una ruta, demostrando que el mecanismo funciona antes de expandirlo a los otros 5 tramos."
    - "El tramo 'desvío detectado→solicitud de recálculo' (4 de 6) queda instrumentado dentro del mismo ciclo de seguimiento GPS que ya decide cuándo recalcular."
  artifacts:
    - frontend/src/utilidades/diagnosticoLatencias.js
    - frontend/src/utilidades/diagnosticoLatencias.test.js
  key_links:
    - "useNavegacion.js calcular() -> diagnosticoLatencias.marcar/medir -> Performance API del navegador -> window.__diagnosticoLatencias.resumen() (consumido en Plan 01-05 por el humano en el dispositivo físico)"
  prohibitions:
    - "La instrumentación no debe registrar ni persistir coordenadas GPS, rumbo o geometría de ruta -- ni siquiera transitoriamente -- en marks, measures, consola o cualquier artefacto generado."
    - "La instrumentación no debe seguir activa/ejecutándose en builds de producción -- cada llamada a marcar()/medir() debe comprobar import.meta.env.DEV en el momento de la llamada, no cachear el valor al cargar el módulo."
    - "Esta fase no debe modificar el comportamiento de navegación en producción para corregir los síntomas diagnosticados -- Fase 1 es solo diagnóstico; cualquier arreglo trivial detectado se documenta como hallazgo para Fase 2/3/4, no se implementa aquí."
  flagged_assumptions:
    - "El edge-probe determinista (specless-probe-fallback) devolvió DIAG-01 y DIAG-02 como 'unclassified/unresolved' -- no tiene heurística para requirements de diagnóstico/medición. No se resolvió con backstop (prohibido por protocolo); los truths y prohibitions de este plan y de 01-02/01-03/01-04/01-05 son la resolución manual, derivada directamente de los Success Criteria de ROADMAP.md Fase 1 y de las decisiones D-01..D-04 de 01-CONTEXT.md."
---

<objective>
Construir el mecanismo de instrumentación dev-only (performance.mark/performance.measure, por D-04 de 01-CONTEXT.md) como un módulo utilitario reutilizable, y cablearlo end-to-end en el primer tramo de latencia (tracer) para probar que el mecanismo funciona antes de expandirlo a los 5 tramos restantes en los planes 01-03 y 01-04. Este plan también cablea el tramo 4 ("desvío detectado→solicitud de recálculo"), que vive enteramente dentro del mismo archivo (useNavegacion.js) que el tracer, y deja la marca de arranque del tramo 6 ("respuesta→ruta renderizada") lista para que 01-04 la empareje contra InteractiveMap.jsx.

Purpose: Fase 1 es de diagnóstico puro (ver 01-CONTEXT.md <domain>) -- este plan NO corrige comportamiento de navegación, solo mide. La utilidad centralizada evita que cada archivo reinvente su propio guard de import.meta.env.DEV y sus propios nombres de marca, lo que reduciría el riesgo de typos entre el mark de inicio y el de fin de cada tramo.
Output: frontend/src/utilidades/diagnosticoLatencias.js (marcar/medir/resumen + registros de nombres MARCAS/TRAMOS), useNavegacion.js instrumentado en los tramos 4 y 5 (con el arranque del tramo 6 listo), y los tests correspondientes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-diagn-stico-e-instrumentaci-n/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@frontend/src/hooks/useNavegacion.js
@frontend/src/hooks/useNavegacion.test.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: diagnosticoLatencias utility + tracer wiring of "solicitud→respuesta ArcGIS" (tramo 5/6)</name>
  <reversibility rating="reversible">D-04 (01-CONTEXT.md): instrumentación dev-only vía performance.mark/measure -- se puede quitar o cambiar de mecanismo sin afectar producción ni otros artefactos. Rating tomado directamente de la decisión, no re-derivado.</reversibility>
  <files>frontend/src/utilidades/diagnosticoLatencias.js, frontend/src/utilidades/diagnosticoLatencias.test.js, frontend/src/hooks/useNavegacion.js</files>
  <read_first>
    frontend/src/hooks/useNavegacion.js (función calcular(), líneas ~110-144: el ciclo completo request/response que ya envuelve rutasApi.resolver); frontend/src/hooks/useNavegacion.test.js (patrón de mocking existente: vi.mock('../utilidades/api', ...), renderHook + act + waitFor); frontend/src/utilidades/api.js si existe (para confirmar la forma exacta de rutasApi.resolver); 01-CONTEXT.md sección <decisions> D-04 (mecanismo exacto) y <code_context> (anclajes sugeridos).
  </read_first>
  <behavior>
    - marcar(nombre): si import.meta.env.DEV es false (comprobado en cada llamada, nunca cacheado), no hace nada; si es true, llama performance.mark(nombre) dentro de try/catch.
    - medir(tramo, inicio, fin): mismo guard DEV; llama performance.measure(tramo, inicio, fin) dentro de try/catch (una medida contra una marca inexistente no debe lanzar fuera de la función); si tiene éxito, hace console.debug('[diag]', tramo, duración redondeada en ms) -- nunca coordenadas.
    - resumen(): agrupa performance.getEntriesByType('measure') filtrando solo nombres que empiezan con 'diag:' en {count, avgMs, minMs, maxMs} (campos numéricos únicamente, sin lat/lng/heading), limpia con performance.clearMarks()/clearMeasures() tras leer, y devuelve el objeto agrupado.
    - Con DEV=true, window.__diagnosticoLatencias queda expuesto con { resumen, MARCAS, TRAMOS }; con DEV=false no se asigna nada a window.
    - Wired en useNavegacion.calcular(): al invocar calcular() y dejar que rutasApi.resolver() resuelva, deben dispararse -- en este orden -- marcar(MARCAS.SOLICITUD_ENVIADA), marcar(MARCAS.RESPUESTA_RECIBIDA), medir(TRAMOS.SOLICITUD_RESPUESTA_ARCGIS, MARCAS.SOLICITUD_ENVIADA, MARCAS.RESPUESTA_RECIBIDA).
  </behavior>
  <action>
    Crear frontend/src/utilidades/diagnosticoLatencias.js exportando dos registros de nombres congelados: MARCAS (nombres de marca puntual: GPS_ACEPTADO='gps:aceptado', MARCADOR_RENDER='marcador:render-inicio', CAMARA_ACTUALIZADA='camara:actualizada', ORIENTACION_CAMBIO='orientacion:cambio', FLECHA_RENDER='flecha:render', DESVIO_DETECTADO='desvio:detectado', RECALCULO_SOLICITADO='recalculo:solicitado', SOLICITUD_ENVIADA='solicitud:enviada', RESPUESTA_RECIBIDA='respuesta:recibida', RUTA_RENDERIZADA='ruta:renderizada') y TRAMOS (nombres de medida, todos con prefijo 'diag:': GPS_MARCADOR='diag:gps-marcador', ORIENTACION_FLECHA='diag:orientacion-flecha', GPS_CAMARA='diag:gps-camara', DESVIO_SOLICITUD='diag:desvio-solicitud', SOLICITUD_RESPUESTA_ARCGIS='diag:solicitud-respuesta-arcgis', RESPUESTA_RUTA_RENDERIZADA='diag:respuesta-ruta-renderizada'). Implementar marcar(nombre), medir(tramo, inicio, fin) y resumen() exactamente como describe <behavior>. El guard de DEV se evalúa dentro de cada función (no como constante de módulo) para que sea determinista bajo test. Seguir las convenciones ya usadas en el repo: comentarios en español explicando el "por qué" (patrón visto en useNavegacion.js líneas 23-32), nombres camelCase con raíces en español.
    Después, importar { marcar, medir, MARCAS, TRAMOS } en useNavegacion.js. Dentro de calcular(), justo después de la línea `ultimoCalculoRef.current = Date.now();` (y antes de `if (esRecalculo) setRecalculando(true);`), llamar marcar(MARCAS.SOLICITUD_ENVIADA) -- colocarla ahí, tras el guard de `calculandoRef.current`, evita marcar invocaciones descartadas por solape. Inmediatamente después de que `const cruda = await rutasApi.resolver(...)` resuelva (antes de `const preparada = prepararRuta(cruda)`), llamar marcar(MARCAS.RESPUESTA_RECIBIDA) y luego medir(TRAMOS.SOLICITUD_RESPUESTA_ARCGIS, MARCAS.SOLICITUD_ENVIADA, MARCAS.RESPUESTA_RECIBIDA). Esta medición cubre el viaje completo frontend→backend→ArcGIS→frontend, no solo el tiempo interno de ArcGIS -- documentar ese alcance en el informe de Fase 1 (Plan 01-05), no inventar una medición separada del lado del backend en este plan.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/utilidades/diagnosticoLatencias.test.js src/hooks/useNavegacion.test.js</automated>
  </verify>
  <done>diagnosticoLatencias.js exporta marcar/medir/resumen/MARCAS/TRAMOS con el guard DEV verificado por test (no-op con DEV=false, activo con DEV=true); useNavegacion.calcular() dispara los tres marks/measure del tramo 5 en un test tipo renderHook que resuelve una ruta mockeada end-to-end.</done>
  <acceptance_criteria>
    - Un test con import.meta.env.DEV mockeado a false demuestra que performance.mark y performance.measure NO se llaman (spy en performance.mark/performance.measure con 0 invocaciones).
    - Un test con DEV=true y performance.mark/measure espiados demuestra que marcar/medir sí se llaman con los nombres exactos definidos en MARCAS/TRAMOS.
    - resumen() en un test devuelve un objeto cuyas claves de cada tramo son exactamente {count, avgMs, minMs, maxMs} (verificado con Object.keys) -- ninguna clave de coordenada o posición.
    - Un test end-to-end sobre useNavegacion() (siguiendo el patrón de useNavegacion.test.js: vi.mock de '../utilidades/api' y de './useGeolocation', renderHook + act(() => result.current.iniciar(...)) + waitFor) espía performance.mark/measure y confirma que, tras resolver la ruta mockeada, se registró una measure llamada 'diag:solicitud-respuesta-arcgis'.
    - `npx vitest run src/utilidades/diagnosticoLatencias.test.js src/hooks/useNavegacion.test.js` termina en verde.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: instrumentar tramo 4 (desvío→solicitud) y marca de arranque del tramo 6 (respuesta recibida)</name>
  <files>frontend/src/hooks/useNavegacion.js, frontend/src/hooks/useNavegacion.test.js</files>
  <read_first>
    frontend/src/hooks/useNavegacion.js (bucle de seguimiento, líneas ~197-257, en particular el bloque "Detección de desvío y recálculo automático" ~246-256 que usa lecturasFueraRef, suficientesLecturas, pasoElTiempo); .planning/codebase/CONCERNS.md sección "Fragile Areas" > "Route Deviation and Recalculation Logic" (por qué existen los 3-lecturas/15s -- este plan NO los toca, solo los mide).
  </read_first>
  <behavior>
    - La primera lectura que cruza el umbral de desviación (lecturasFueraRef.current pasa de 0 a 1) dispara marcar(MARCAS.DESVIO_DETECTADO) una sola vez por ciclo de desviación, no en cada lectura subsiguiente fuera de ruta.
    - Justo antes de invocar calcular(position, destino, modo, true) para el recálculo real (cuando suficientesLecturas && pasoElTiempo), se dispara marcar(MARCAS.RECALCULO_SOLICITADO) y medir(TRAMOS.DESVIO_SOLICITUD, MARCAS.DESVIO_DETECTADO, MARCAS.RECALCULO_SOLICITADO).
    - MARCAS.RESPUESTA_RECIBIDA (ya añadida en Task 1 dentro de calcular()) sirve también como arranque del tramo 6 ("respuesta→ruta renderizada") -- este task NO añade el extremo final del tramo 6 (vive en InteractiveMap.jsx, Plan 01-04); solo confirma que la marca de arranque ya emitida es reutilizable sin cambios.
  </behavior>
  <action>
    En el bloque `if (ubicacion.desviacionM > UMBRAL_DESVIO_M) { lecturasFueraRef.current += 1; ... }` de useNavegacion.js, inmediatamente después de `lecturasFueraRef.current += 1;`, si el valor incrementado es 1, llamar marcar(MARCAS.DESVIO_DETECTADO). Dentro del `if (suficientesLecturas && pasoElTiempo) { lecturasFueraRef.current = 0; calcular(position, destino, modo, true); }`, justo antes de la llamada a calcular(...), añadir marcar(MARCAS.RECALCULO_SOLICITADO) y, justo después, medir(TRAMOS.DESVIO_SOLICITUD, MARCAS.DESVIO_DETECTADO, MARCAS.RECALCULO_SOLICITADO). Importar marcar/medir/MARCAS/TRAMOS ya deberían estar disponibles desde el import añadido en Task 1 -- extenderlo si hace falta.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useNavegacion.test.js</automated>
  </verify>
  <done>El ciclo de seguimiento marca desvío detectado una sola vez por ciclo y mide el tramo hasta que se solicita el recálculo, sin alterar UMBRAL_DESVIO_M, LECTURAS_PARA_RECALCULAR ni ESPERA_ENTRE_RECALCULOS_MS.</done>
  <acceptance_criteria>
    - Un test que simula 3 lecturas GPS consecutivas fuera de ruta (siguiendo el patrón de rerender() ya usado en useNavegacion.test.js) confirma exactamente UNA llamada a performance.mark con 'desvio:detectado', no tres.
    - El mismo test confirma que, en la lectura que dispara el recálculo real, se registra una measure 'diag:desvio-solicitud'.
    - Un `git diff` de useNavegacion.js confirma que UMBRAL_DESVIO_M, LECTURAS_PARA_RECALCULAR y ESPERA_ENTRE_RECALCULOS_MS permanecen con sus valores originales (45, 3, 15000).
    - `npx vitest run src/hooks/useNavegacion.test.js` termina en verde.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DevTools console / window.__diagnosticoLatencias | Diagnostic data surface exposed to whoever can open the console on the page (dev-only, but any script running in-page could also read it) |
| Performance timeline buffer (browser-internal) | Unbounded-by-default buffer of marks/measures accumulated during a live navigation session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | diagnosticoLatencias.js (marcar/medir/resumen) | high | mitigate | Mark/measure/resumen APIs accept and expose only tramo-name strings and numeric durations -- never lat/lng/heading/route data; resumen()'s output shape is asserted by test to contain only {count, avgMs, minMs, maxMs} keys |
| T-01-02 | Information Disclosure / residual exposure | window.__diagnosticoLatencias in production | medium | mitigate | Every marcar()/medir() call re-checks import.meta.env.DEV live (never cached at module load) so Vite's static replacement can dead-code-eliminate the guarded branches from production bundles; behavior verified by a DEV=false unit test asserting zero performance.mark/measure calls |
| T-01-03 | Denial of Service (resource growth) | Browser Performance timeline buffer | low | mitigate | resumen() calls performance.clearMarks()/clearMeasures() after aggregating, bounding buffer growth during a long physical-device capture session (Plan 01-05) |
</threat_model>

<verification>
Run `cd frontend && npx vitest run src/utilidades/diagnosticoLatencias.test.js src/hooks/useNavegacion.test.js` -- all tests pass, including the DEV-gating tests and the end-to-end tracer test for tramo 5.
</verification>

<success_criteria>
- diagnosticoLatencias.js exists with marcar/medir/resumen/MARCAS/TRAMOS, fully unit-tested including the DEV=false no-op path.
- useNavegacion.js emits real performance marks/measures for tramo 5 (solicitud→respuesta ArcGIS) and tramo 4 (desvío→solicitud), and carries the reusable start-mark for tramo 6.
- No change to navigation behavior or recalculation thresholds -- diagnosis-only, per D-01..D-04 and the Fase-1-is-diagnosis-only constraint.
</success_criteria>

<output>
Create `.planning/phases/01-diagn-stico-e-instrumentaci-n/01-01-SUMMARY.md` when done
</output>
