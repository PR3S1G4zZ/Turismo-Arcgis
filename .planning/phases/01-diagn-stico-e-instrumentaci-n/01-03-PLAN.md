---
phase: 01-diagn-stico-e-instrumentaci-n
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/src/hooks/useGeolocation.js
  - frontend/src/hooks/useGeolocation.test.js
  - frontend/src/hooks/useOrientacion.js
  - frontend/src/hooks/useOrientacion.test.js
autonomous: true
requirements: [DIAG-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Cada fijacion GPS aceptada (esConfiable=true) dispara la marca de arranque compartida por los tramos 1 (GPS->marcador) y 3 (GPS->camara), sin marcar las fijaciones descartadas por baja precision o timestamp obsoleto."
    - "Cada cambio de rumbo de brujula que supera el throttle de ~10 Hz dispara la marca de arranque del tramo 2 (orientacion->flecha)."
    - "useOrientacion.js queda con cobertura de test por primera vez (no existia useOrientacion.test.js antes de este plan)."
  artifacts:
    - frontend/src/hooks/useOrientacion.test.js
  key_links:
    - "useGeolocation.js handleSuccess() -> diagnosticoLatencias.marcar(MARCAS.GPS_ACEPTADO) -> InteractiveMap.jsx (Plan 01-04 empareja el extremo final de los tramos 1 y 3)"
    - "useOrientacion.js manejar() -> diagnosticoLatencias.marcar(MARCAS.ORIENTACION_CAMBIO) -> InteractiveMap.jsx (Plan 01-04 empareja el extremo final del tramo 2)"
  prohibitions:
    - "La instrumentacion no debe registrar coordenadas GPS, rumbo o geometria de ruta -- solo el nombre de marca fijo, nunca datos interpolados en el nombre."
    - "La instrumentacion no debe seguir activa en produccion -- import.meta.env.DEV se comprueba en cada llamada a marcar(), reutilizando diagnosticoLatencias.js de Plan 01-01 sin duplicar el guard."
---

<objective>
Cablear las marcas de arranque de los tramos 1 (GPS aceptado->marcador), 2 (orientacion->rotacion de flecha) y 3 (GPS aceptado->actualizacion de camara) usando la utilidad diagnosticoLatencias.js creada en Plan 01-01. Este plan solo añade marcar() en los hooks de origen (useGeolocation.js, useOrientacion.js); los extremos finales (donde realmente se pinta el marcador/la flecha/la camara) viven en InteractiveMap.jsx y se cablean en Plan 01-04, que depende de este plan para tener algo que emparejar.

Purpose: expandir la instrumentacion probada por el tracer de Plan 01-01 a los tramos que dependen de sensores del dispositivo (GPS, brujula), sin tocar el comportamiento real de lectura de GPS/orientacion.
Output: useGeolocation.js y useOrientacion.js instrumentados; useOrientacion.test.js creado desde cero (Nyquist: no existia cobertura previa).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-diagn-stico-e-instrumentaci-n/01-CONTEXT.md
@frontend/src/hooks/useGeolocation.js
@frontend/src/hooks/useGeolocation.test.js
@frontend/src/hooks/useOrientacion.js
@frontend/src/utilidades/diagnosticoLatencias.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: marca de arranque compartida GPS_ACEPTADO en useGeolocation.js (tramos 1 y 3)</name>
  <files>frontend/src/hooks/useGeolocation.js, frontend/src/hooks/useGeolocation.test.js</files>
  <read_first>
    frontend/src/hooks/useGeolocation.js funcion handleSuccess (dentro de iniciarSeguimiento, ~linea 112-163): el guard `if (!esConfiable) { ...; return; }` seguido del calculo de rumboCrudo y la llamada final a setPosition(...); frontend/src/hooks/useGeolocation.test.js (patron de test: Object.defineProperty(navigator, 'geolocation', ...), success()/failure() capturados del watchPosition mockeado); frontend/src/utilidades/diagnosticoLatencias.js (creado en Plan 01-01: exports marcar, MARCAS).
  </read_first>
  <behavior>
    - Cuando handleSuccess recibe una fijacion que pasa el guard esConfiable (la rama que llega a setPosition), se dispara marcar(MARCAS.GPS_ACEPTADO) exactamente una vez.
    - Cuando handleSuccess descarta la fijacion (esConfiable es false, la rama de early return), NO se dispara marcar.
  </behavior>
  <action>
    Importar { marcar, MARCAS } desde '../utilidades/diagnosticoLatencias' en useGeolocation.js. Dentro de handleSuccess, justo antes de la llamada a setPosition({...}) (la que publica lat/lng/heading/speed), añadir marcar(MARCAS.GPS_ACEPTADO). Esta marca sirve de arranque compartido para dos tramos distintos (GPS->marcador y GPS->camara) -- ambos extremos finales se emparejan en InteractiveMap.jsx en Plan 01-04, no en este plan.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useGeolocation.test.js</automated>
  </verify>
  <done>handleSuccess marca cada fijacion aceptada exactamente una vez y ninguna fijacion descartada, verificado por test con performance.mark espiado.</done>
  <acceptance_criteria>
    - Un test que envia una fijacion con accuracy=51 (por encima de PRECISION_MAXIMA_M=50, ya cubierto por el test existente "rejects a fix whose accuracy radius exceeds 50 metres") confirma CERO llamadas a performance.mark('gps:aceptado').
    - Un test que envia una fijacion valida confirma exactamente UNA llamada a performance.mark('gps:aceptado').
    - `npx vitest run src/hooks/useGeolocation.test.js` termina en verde, incluyendo los 5 tests preexistentes sin regresiones.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: marca ORIENTACION_CAMBIO en useOrientacion.js + test nuevo (tramo 2)</name>
  <files>frontend/src/hooks/useOrientacion.js, frontend/src/hooks/useOrientacion.test.js</files>
  <read_first>
    frontend/src/hooks/useOrientacion.js completo (88 lineas): funcion manejar (callback de deviceorientation/deviceorientationabsolute, ~linea 40-50) con el throttle de ~100ms via ultimoEmitRef y la llamada final a setHeading(Math.round(suavRef.current)); no existe useOrientacion.test.js todavia -- crearlo desde cero siguiendo el patron de useGeolocation.test.js (Object.defineProperty o window.dispatchEvent para simular deviceorientation, renderHook + act).
  </read_first>
  <behavior>
    - Cuando manejar() procesa un evento de orientacion que pasa el throttle de 100ms (la rama que llega a setHeading), se dispara marcar(MARCAS.ORIENTACION_CAMBIO) exactamente una vez por evento aceptado.
    - Un evento que cae dentro de la ventana de throttle (menos de 100ms desde el ultimo aceptado) NO dispara marcar.
    - Un evento sin rumbo valido (leerRumbo devuelve null) NO dispara marcar.
  </behavior>
  <action>
    Importar { marcar, MARCAS } desde '../utilidades/diagnosticoLatencias' en useOrientacion.js. Dentro de manejar(), justo antes de la llamada a setHeading(Math.round(suavRef.current)) (despues de que el guard `if (ahora - ultimoEmitRef.current < 100) return;` ya haya pasado), añadir marcar(MARCAS.ORIENTACION_CAMBIO). Crear frontend/src/hooks/useOrientacion.test.js desde cero: mockear window.DeviceOrientationEvent como no-funcion (para forzar la rama sin permiso explicito, requierePermiso=false, que arranca sola sin gesto) y disparar window.dispatchEvent(new Event('deviceorientation')) con las propiedades absolute/alpha asignadas manualmente sobre el evento antes de dispatch (jsdom no construye eventos de orientacion nativos), verificando heading actualizado y performance.mark llamado con 'orientacion:cambio'.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useOrientacion.test.js</automated>
  </verify>
  <done>useOrientacion.js marca cada cambio de rumbo aceptado (post-throttle, post-validacion) y useOrientacion.test.js existe con cobertura minima del hook, algo que no existia antes de este plan (Nyquist scaffold).</done>
  <acceptance_criteria>
    - Un test con dos eventos deviceorientation disparados con menos de 100ms de diferencia confirma UNA sola llamada a performance.mark('orientacion:cambio'), no dos.
    - Un test con un evento sin alpha/webkitCompassHeading validos (leerRumbo devuelve null) confirma CERO llamadas a performance.mark.
    - `npx vitest run src/hooks/useOrientacion.test.js` termina en verde.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device sensors (GPS, compass) -> React state | Same boundary useGeolocation.js/useOrientacion.js already cross; instrumentation adds no new boundary, only observes accepted transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | useGeolocation.js / useOrientacion.js marcar() calls | high | mitigate | Both call sites pass only the static MARCAS.GPS_ACEPTADO / MARCAS.ORIENTACION_CAMBIO string constants imported from diagnosticoLatencias.js -- never lat/lng/heading values -- verified by test asserting the mark name argument, not by inspection |
| T-01-02 | Information Disclosure / residual exposure | Same DEV guard as Plan 01-01, reused not reimplemented | medium | mitigate | No new DEV-gating logic is introduced in this plan -- both hooks import the already-tested marcar() from diagnosticoLatencias.js, inheriting its live import.meta.env.DEV check |
</threat_model>

<verification>
`cd frontend && npx vitest run src/hooks/useGeolocation.test.js src/hooks/useOrientacion.test.js` -- all tests pass, including the new useOrientacion.test.js scaffold.
</verification>

<success_criteria>
- useGeolocation.js marks every accepted GPS fix exactly once via the shared GPS_ACEPTADO mark.
- useOrientacion.js marks every accepted (post-throttle, post-validation) compass reading via ORIENTACION_CAMBIO.
- useOrientacion.js has test coverage for the first time.
- No change to GPS acceptance thresholds, throttle timing, or heading smoothing behavior.
</success_criteria>

<output>
Create `.planning/phases/01-diagn-stico-e-instrumentaci-n/01-03-SUMMARY.md` when done
</output>
