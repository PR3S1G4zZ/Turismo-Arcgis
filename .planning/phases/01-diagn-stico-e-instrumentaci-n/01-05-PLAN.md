---
phase: 01-diagn-stico-e-instrumentaci-n
plan: 05
type: execute
wave: 4
depends_on: ["01-02", "01-04"]
files_modified:
  - .planning/phases/01-diagn-stico-e-instrumentaci-n/01-DIAGNOSIS.md
autonomous: false
requirements: [DIAG-01]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Existen instrucciones de captura paso a paso (build en modo development para no perder la instrumentacion, acceso a DevTools remoto en Android Chrome / iPhone Safari, flujo de usuario que dispara los 6 tramos, lectura de window.__diagnosticoLatencias.resumen()) escritas antes de pedirle al humano que ejecute nada -- nada queda sin automatizar que Claude pudiera automatizar."
    - "La tabla de latencias y la clasificacion de causa raiz de 01-DIAGNOSIS.md quedan completas con numeros reales de al menos un Android fisico tras este plan (ROADMAP.md Fase 1 Success Criteria #1 y #2)."
    - "El build de captura en modo development se revierte a un build de produccion normal despues de la sesion, sin dejar la instrumentacion expuesta en staging de forma permanente."
  artifacts: []
  key_links:
    - "01-DIAGNOSIS.md (esqueleto de Plan 01-02) + resumen() reportado por el humano (Plan 01-01/03/04) -> 01-DIAGNOSIS.md completo (este plan)"
  prohibitions:
    - "No pedirle al humano nada que Claude pueda automatizar -- el build, la escritura de instrucciones y la sintesis final del informe son tareas 'auto'; solo caminar fisicamente con el telefono y leer la consola remota del navegador queda para el humano, porque ningun agente puede operar un telefono fisico."
    - "El reporte del humano no debe incluir coordenadas, capturas de pantalla del mapa/ruta ni tokens -- solo la tabla numerica de window.__diagnosticoLatencias.resumen()."
---

<objective>
Cerrar Fase 1: producir un build reproducible que conserve la instrumentacion (modo development, ya que un build de produccion normal la elimina por diseño), escribir instrucciones de captura concretas y accionables para el dueño del milestone (que ya confirmo tener Android real, iPhone si aplica, y entorno HTTPS de staging listos, per 01-CONTEXT.md <specifics>), pedirle que ejecute la captura y reporte los numeros, y sintetizar esos numeros en la tabla de latencias y la clasificacion de causa raiz de 01-DIAGNOSIS.md.

Purpose: ningun agente puede caminar con un telefono fisico ni conducir un auto -- esta es la unica pieza de Fase 1 que exige una accion humana real, per la nota de discrecion de 01-CONTEXT.md ("el plan de Fase 1 debe proponer un mecanismo concreto... y el usuario ejecuta la medicion y reporta resultados").
Output: 01-DIAGNOSIS.md completo (auditoria de Plan 01-02 + tabla de latencias real + causa raiz clasificada), cumpliendo los 3 Success Criteria de ROADMAP.md Fase 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-diagn-stico-e-instrumentaci-n/01-CONTEXT.md
@.planning/phases/01-diagn-stico-e-instrumentaci-n/01-DIAGNOSIS.md
@frontend/src/utilidades/diagnosticoLatencias.js
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: build de captura en modo development + instrucciones paso a paso</name>
  <files>.planning/phases/01-diagn-stico-e-instrumentaci-n/01-DIAGNOSIS.md</files>
  <read_first>
    Dockerfile (confirma que el deploy normal corre `npm run build` sin flags, es decir modo production con import.meta.env.DEV=false -- por eso hace falta un build especial para esta captura); frontend/package.json scripts (build: "vite build", dev: "vite"); frontend/src/utilidades/diagnosticoLatencias.js (confirma la API real de window.__diagnosticoLatencias.resumen() creada en Plan 01-01); 01-CONTEXT.md seccion specifics (confirma que el usuario ya tiene Android real y entorno HTTPS de staging listos).
  </read_first>
  <action>
    Ejecutar `cd frontend && npx vitest run` (suite completa) para confirmar que toda la instrumentacion de Plan 01-01/01-03/01-04 sigue en verde junta, no solo archivo por archivo. Ejecutar `cd frontend && npx vite build --mode development` para confirmar que un build en modo development compila sin errores (este flag mantiene import.meta.env.DEV=true, a diferencia del build normal `npm run build` que usa modo production y elimina la instrumentacion por diseño -- documentar esta distincion explicitamente en el informe). Añadir a .planning/phases/01-diagn-stico-e-instrumentaci-n/01-DIAGNOSIS.md una seccion "## Instrucciones de captura en dispositivo fisico" con los pasos, en orden: (1) generar el build de captura con `npx vite build --mode development` desde frontend/, (2) desplegar ese build al entorno HTTPS de staging ya existente siguiendo el mismo proceso de despliegue habitual, sustituyendo unicamente el comando de build por el del paso 1 para ese despliegue puntual; (3) en Android, abrir Chrome, navegar a la URL de staging, conectar el telefono por USB y abrir chrome://inspect en un Chrome de escritorio para obtener la consola remota de esa pestaña (o, si hay iPhone disponible, conectarlo a un Mac y usar Safari > Develop > [dispositivo] > [pestaña] para la consola remota); (4) en la app, abrir el detalle de un sitio, pulsar "Fijar Ruta de Destino" y aceptar el permiso de ubicacion (dispara los tramos GPS->marcador y GPS->camara al llegar la primera fijacion, y solicitud->respuesta ArcGIS + respuesta->ruta renderizada al calcular la ruta inicial); (5) empezar a caminar o conducir siguiendo la ruta, dejando que el telefono gire libremente en la mano en algun momento para forzar lecturas de brujula (dispara orientacion->flecha), y desviarse deliberadamente de la ruta por mas de 45 metros durante al menos unos segundos para forzar el recalculo (dispara desvio->solicitud); (6) repetir el paso 4-5 un par de veces si es posible para acumular varias mediciones por tramo; (7) en la consola remota, ejecutar `window.__diagnosticoLatencias.resumen()` y copiar el objeto resultante (son solo numeros y nombres de tramo, nunca coordenadas); (8) IMPORTANTE -- inmediatamente despues de terminar la captura, volver a desplegar el build de produccion normal (`npm run build`, sin --mode) en el mismo entorno de staging, para no dejar la instrumentacion de desarrollo expuesta de forma permanente. Cerrar la seccion pidiendo explicitamente que el resultado a reportar es SOLO la salida de resumen() (o una transcripcion de sus numeros), nunca una captura de pantalla del mapa o la ruta real.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run && npx vite build --mode development && test -d dist</automated>
  </verify>
  <done>La suite completa de tests pasa junta, un build en modo development compila sin errores, y 01-DIAGNOSIS.md tiene una seccion de instrucciones de captura completa, numerada y accionable, incluyendo el paso de revertir a build de produccion al terminar.</done>
  <acceptance_criteria>
    - `npx vitest run` (suite completa del frontend) termina en verde, no solo los archivos tocados por esta fase.
    - `npx vite build --mode development` termina con exit code 0 y genera frontend/dist/.
    - La seccion "## Instrucciones de captura en dispositivo fisico" en 01-DIAGNOSIS.md incluye los 8 pasos descritos en <action>, incluyendo explicitamente el paso de revertir al build de produccion normal al finalizar.
    - Ningun paso le pide al humano automatizar algo que Claude podria hacer (el build, el test y la escritura de instrucciones ya estan hechos por este task; solo caminar/conducir con el telefono y leer la consola remota quedan para el humano).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: el dueño del milestone ejecuta la captura fisica y reporta resumen()</name>
  <precondition>Existe un Android fisico (iPhone opcional) y un entorno HTTPS de staging ya desplegable, per confirmacion del usuario en 01-CONTEXT.md seccion specifics ("el usuario confirmo tener dispositivos fisicos... y entorno HTTPS de staging listos").</precondition>
  <action>Ejecutar la captura de latencias en al menos un Android fisico y reportar los numeros de window.__diagnosticoLatencias.resumen() -- esto requiere caminar/conducir con el telefono real, algo que ningun agente puede automatizar.</action>
  <instructions>
    Ya automatice todo lo previo: el build de captura en modo development compila (Task 1), y la seccion "## Instrucciones de captura en dispositivo fisico" de .planning/phases/01-diagn-stico-e-instrumentaci-n/01-DIAGNOSIS.md tiene los 8 pasos completos (build, despliegue a staging, DevTools remoto, flujo de usuario que dispara los 6 tramos, lectura de resumen(), reversion a build de produccion).
    Lo unico que falta es que sigas esos 8 pasos en tu Android real (iPhone si tienes uno disponible) y me reportes aqui la salida de window.__diagnosticoLatencias.resumen() -- tramo, count, avgMs, minMs, maxMs por cada uno de los 6 tramos. No reportes coordenadas ni capturas de pantalla del mapa o la ruta.
  </instructions>
  <verification>Los numeros reportados tienen las 6 claves de tramo esperadas (diag:gps-marcador, diag:orientacion-flecha, diag:gps-camara, diag:desvio-solicitud, diag:solicitud-respuesta-arcgis, diag:respuesta-ruta-renderizada) y ninguna contiene coordenadas.</verification>
  <resume-signal>Pega aqui la salida de window.__diagnosticoLatencias.resumen() (o transcribe sus numeros) para continuar.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: sintetizar los numeros reportados en la tabla de latencias y la causa raiz</name>
  <files>.planning/phases/01-diagn-stico-e-instrumentaci-n/01-DIAGNOSIS.md</files>
  <read_first>
    .planning/phases/01-diagn-stico-e-instrumentaci-n/01-DIAGNOSIS.md secciones "## Tabla de latencias" y "## Causa raiz por retraso" (esqueleto de Plan 01-02, con los candidatos internos/sensor ya listados por tramo); los numeros de resumen() reportados por el usuario en el checkpoint del Task 2 de este mismo plan.
  </read_first>
  <action>
    Usando UNICAMENTE los numeros que el usuario reporto en el checkpoint (Task 2) -- nunca inventar ni estimar valores --, reemplazar las columnas pendientes de "## Tabla de latencias" con Mediciones (n), Promedio (ms), Minimo (ms) y Maximo (ms) reales por cada uno de los 6 tramos, indicando el dispositivo (p. ej. "Android real"). En "## Causa raiz por retraso", para cada uno de los 6 tramos, añadir una linea "Clasificacion: interno" o "Clasificacion: sensor/plataforma" (o ambas si el numero sugiere una combinacion) justificada con el numero medido contra los candidatos ya listados en el esqueleto (por ejemplo: si el tramo desvio->solicitud promedia cerca de 15000ms, la clasificacion es interno, atribuible a ESPERA_ENTRE_RECALCULOS_MS, no a un bug; si el tramo GPS->marcador varia mucho entre mediciones sin relacion aparente con el codigo, la clasificacion es sensor/plataforma, atribuible a la frecuencia real de watchPosition). Si el usuario no reporto datos para un tramo especifico, dejar ese tramo marcado explicitamente como "sin datos -- pendiente de una sesion de captura adicional" en vez de inventar un numero.
  </action>
  <verify>
    <automated>cd "C:/Users/MediaLab/orca/workspaces/Turismo-Arcgis/leatherback" && [ "$(grep -c 'Clasificacion:' .planning/phases/01-diagn-stico-e-instrumentaci-n/01-DIAGNOSIS.md)" -ge 6 ]</automated>
  </verify>
  <done>01-DIAGNOSIS.md tiene una tabla de latencias con datos reales (o marcados explicitamente como sin datos) y una clasificacion de causa raiz para cada uno de los 6 tramos, cumpliendo ROADMAP.md Fase 1 Success Criteria #1 y #2.</done>
  <acceptance_criteria>
    - Cada uno de los 6 tramos tiene al menos una linea "Clasificacion: interno" o "Clasificacion: sensor/plataforma" en la seccion de causa raiz, o esta explicitamente marcado como "sin datos" si el humano no lo reporto.
    - Ningun numero en la tabla de latencias fue inventado -- todos provienen textualmente de lo reportado por el usuario en el checkpoint del Task 2.
    - El informe completo (.planning/phases/01-diagn-stico-e-instrumentaci-n/01-DIAGNOSIS.md) contiene, en un solo archivo: la auditoria PR#5 (Plan 01-02), la tabla de latencias real y la clasificacion de causa raiz -- los 3 Success Criteria de ROADMAP.md Fase 1.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Development-mode build deployed to shared HTTPS staging | Temporarily exposes dev-only instrumentation (still no coordinates, but timing internals + console.debug noise) to anyone reaching that staging URL during the capture window |
| Human-reported measurement data entering the diagnosis report | Data crosses from an uncontrolled human-paste channel into a committed project artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering (accidental persistent dev exposure) | Staging deployment during capture window | medium | mitigate | Task 1's instructions explicitly include reverting to the normal production build immediately after capture, called out as step 8 and re-stated in the checkpoint's how-to-verify |
| T-01-01 | Information Disclosure | Human-reported resumen() data pasted into 01-DIAGNOSIS.md | high | mitigate | Checkpoint instructions explicitly request only the numeric resumen() table (tramo/count/avgMs/minMs/maxMs), explicitly forbid screenshots of the map/route or coordinates in the report |
</threat_model>

<verification>
01-DIAGNOSIS.md, combined across Plan 01-02 and this plan, contains: the PR#5 audit table (síntoma→commit→estado), a latency table with real device numbers for the 6 tramos (or explicit "sin datos" markers), and a root-cause classification (interno/sensor-plataforma) per tramo -- satisfying all 3 ROADMAP.md Phase 1 Success Criteria in one artifact.
</verification>

<success_criteria>
- A development-mode build was produced and validated to compile.
- The milestone owner executed the physical capture and reported real numbers for at least one Android device.
- 01-DIAGNOSIS.md is complete: audit + latency table + root-cause classification, all in one file, no fabricated numbers.
- Staging was returned to a normal production build after capture.
</success_criteria>

<output>
Create `.planning/phases/01-diagn-stico-e-instrumentaci-n/01-05-SUMMARY.md` when done
</output>
