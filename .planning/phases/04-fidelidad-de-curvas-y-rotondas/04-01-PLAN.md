---
phase: 04-fidelidad-de-curvas-y-rotondas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/utils/arcgisRouting.js
  - backend/src/utils/osrmRouting.js
  - frontend/src/componentes/detalle/InteractiveMap.jsx
  - .planning/phases/04-fidelidad-de-curvas-y-rotondas/04-COMPARACION-GEOMETRIA.md
autonomous: true
requirements: [GEOM-01]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Existe un mecanismo de captura dev-only (guardado por una variable de entorno explícita, nunca activo por defecto) que registra a archivo, sin coordenadas de recorridos personales, las 4 primeras etapas del pipeline: respuesta cruda de ArcGIS, respuesta cruda de OSRM si intervino, objeto normalizado por el backend, y el GeoJSON que se entrega a MapLibre (D-02 de 04-CONTEXT.md)."
    - "El mecanismo está inactivo (cero escrituras a disco) cuando la variable de entorno CAPTURAR_GEOMETRIA no está presente o no vale 'true' -- el comportamiento normal de producción de arcgisRouting.js/osrmRouting.js/InteractiveMap.jsx queda sin alterar."
    - "04-COMPARACION-GEOMETRIA.md queda con un esqueleto que incluye la tabla de las 5 etapas vacía, instrucciones de captura paso a paso completas y accionables, y 1-2 rotondas/curvas candidatas de Itagüí sugeridas -- listo para que el dueño del milestone ejecute la captura en el Plan 04-02 sin tener que inventar el procedimiento."
  artifacts:
    - .planning/phases/04-fidelidad-de-curvas-y-rotondas/04-COMPARACION-GEOMETRIA.md
  key_links:
    - "arcgisRouting.js normalizar() + osrmRouting.js resolverRutaOsrm() -> captura backend (etapas 1-3) -> archivos en .planning/phases/04-fidelidad-de-curvas-y-rotondas/captura/"
    - "InteractiveMap.jsx efecto de ruta.puntos (línea ~319) -> captura frontend (etapa 4, GeoJSON a MapLibre) -> consola DevTools -> archivo pegado a mano por el humano en Plan 04-02"
  prohibitions:
    - "El mecanismo de captura no debe registrar ni persistir la ubicación GPS real del usuario, recorridos completos ni tokens -- solo la geometría de la ruta de prueba explícitamente solicitada entre dos sitios turísticos públicos (constraint de privacidad del milestone, D-03 de 04-CONTEXT.md)."
    - "Este plan no modifica el comportamiento de navegación en producción -- ninguna capa de render, umbral de recálculo ni lógica de geoRuta.js cambia aquí; solo se añade instrumentación apagada por defecto."
    - "No se implementa aún ninguna mejora de representación visual (casing/ancho/opacidad/densificación) -- eso depende de evidencia real que todavía no existe y se decide después del checkpoint humano del Plan 04-02 (D-06 de 04-CONTEXT.md)."
  flagged_assumptions:
    - "GEOM-01 no tiene heurística en el edge-probe determinista del proyecto para requirements de captura/comparación documental (mismo patrón que DIAG-01/DIAG-02 en Fase 1). La resolución manual de este plan y del Plan 04-02 es la traducción directa de ROADMAP.md Fase 4 Success Criteria #1/#2 y de las decisiones D-01/D-02/D-03 de 04-CONTEXT.md."
---

<objective>
Construir el mecanismo de captura dev-only para las primeras 4 de las 5 etapas del pipeline geométrico (ArcGIS crudo, OSRM crudo si interviene, normalizado backend, GeoJSON a MapLibre) y dejar el informe `04-COMPARACION-GEOMETRIA.md` con un esqueleto completo -- tabla vacía + instrucciones de captura paso a paso + candidatas de rotonda -- listo para que el Plan 04-02 se lo entregue al dueño del milestone sin fricción.

Purpose: GEOM-01 exige capturar una ruta real de Itagüí y comparar las 5 etapas ANTES de tocar el renderizado (04-CONTEXT.md <domain>); este plan construye la instrumentación que hace posible esa captura sin exponer nada nuevo en producción (D-01/D-02 de 04-CONTEXT.md).
Output: `backend/src/utils/arcgisRouting.js` y `backend/src/utils/osrmRouting.js` instrumentados con captura guardada por `CAPTURAR_GEOMETRIA=true`; `InteractiveMap.jsx` con un log de consola dev-only equivalente para el GeoJSON de MapLibre; `04-COMPARACION-GEOMETRIA.md` con esqueleto + instrucciones.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-fidelidad-de-curvas-y-rotondas/04-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/INTEGRATIONS.md
@backend/src/utils/arcgisRouting.js
@backend/src/utils/osrmRouting.js
@frontend/src/componentes/detalle/InteractiveMap.jsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: captura dev-only de las etapas 1-3 (ArcGIS crudo, OSRM crudo, normalizado backend)</name>
  <reversibility rating="reversible">Instrumentación guardada por variable de entorno, sin wiring en rutas de producción -- se puede eliminar o desactivar sin afectar el comportamiento de ruteo ni otros artefactos (mismo rating que D-04 de la Fase 1, aplicado aquí a D-02 de 04-CONTEXT.md).</reversibility>
  <files>backend/src/utils/arcgisRouting.js, backend/src/utils/osrmRouting.js</files>
  <read_first>
    backend/src/utils/arcgisRouting.js función `normalizar(data)` (línea ~179, recibe la respuesta cruda de ArcGIS y devuelve `{ fuente: 'arcgis', puntos, pasos, distanciaM, duracionMin }`) y `solve()` (línea ~248, confirma que `outputLines: 'esriNAOutputLineTrueShape'` ya está en los params, línea ~263); backend/src/utils/osrmRouting.js función `resolverRutaOsrm()` (línea ~67, recibe `data` tras `await res.json()` y devuelve `{ fuente: 'osrm', puntos, pasos, distanciaM, duracionMin }`); 04-CONTEXT.md sección `<decisions>` D-02 (mecanismo exacto: 5 etapas a archivo, directorio propio de la fase) y D-03 (solo coordenadas de sitios públicos, nunca ubicación real).
  </read_first>
  <behavior>
    - Nueva función interna `capturarSiCorresponde(nombre, contenido)` en cada archivo (o en un módulo compartido nuevo `backend/src/utils/capturaGeometria.js` importado por ambos -- preferible para no duplicar la lógica): si `process.env.CAPTURAR_GEOMETRIA !== 'true'`, retorna inmediatamente sin tocar el sistema de archivos.
    - Si la variable sí vale `'true'`: crea (si no existe) el directorio `.planning/phases/04-fidelidad-de-curvas-y-rotondas/captura/` resuelto desde `import.meta.url` (no desde `process.cwd()`, que puede ser `backend/` según el script npm), y escribe `contenido` como JSON indentado en un archivo `<timestamp-ISO-sin-caracteres-invalidos>-<nombre>.json`.
    - Toda la lógica de captura va envuelta en try/catch: un fallo al escribir (permisos, disco lleno) se loguea con `console.warn('[captura-geometria]', ...)` pero NUNCA interrumpe ni modifica la respuesta real de ruteo que recibe el usuario.
    - En `arcgisRouting.js`, `normalizar(data)` llama `capturarSiCorresponde('arcgis-crudo', data)` como primera línea de la función (antes de cualquier transformación), y llama `capturarSiCorresponde('arcgis-normalizado', <objeto que va a retornar>)` justo antes del `return`.
    - En `osrmRouting.js`, `resolverRutaOsrm()` llama `capturarSiCorresponde('osrm-crudo', data)` inmediatamente después de `const data = await res.json();`, y `capturarSiCorresponde('osrm-normalizado', <objeto que va a retornar>)` justo antes del `return`.
  </behavior>
  <action>
    Crear `backend/src/utils/capturaGeometria.js` exportando `export function capturarSiCorresponde(nombre, contenido)` con el comportamiento descrito arriba. Usar `import { mkdirSync, writeFileSync } from 'node:fs';`, `import { dirname, join } from 'node:path';`, `import { fileURLToPath } from 'node:url';` para resolver el directorio de captura como `join(dirname(fileURLToPath(import.meta.url)), '..', '..', '..', '.planning', 'phases', '04-fidelidad-de-curvas-y-rotondas', 'captura')` (tres niveles arriba desde `backend/src/utils/` llegan a la raíz del repo). El nombre de archivo usa `new Date().toISOString().replace(/[:.]/g, '-')` como prefijo de timestamp para no pisar capturas anteriores en la misma sesión.
    Importar `capturarSiCorresponde` en `arcgisRouting.js` y cablearla en `normalizar(data)` exactamente en los dos puntos descritos en `<behavior>`. Importarla también en `osrmRouting.js` y cablearla en `resolverRutaOsrm()` en los dos puntos descritos. No modificar ninguna otra función ni el comportamiento de `solve()`, `obtenerToken()`, `elegirModo()` u otras rutas.
  </action>
  <verify>
    <automated>cd "C:/Users/MediaLab/orca/workspaces/Turismo-Arcgis/phase-4-geometria" && node --check backend/src/utils/capturaGeometria.js && node --check backend/src/utils/arcgisRouting.js && node --check backend/src/utils/osrmRouting.js && node -e "process.env.CAPTURAR_GEOMETRIA=''; import('./backend/src/utils/capturaGeometria.js').then(m => { let escribio = false; const fs = require('node:fs'); const orig = fs.writeFileSync; fs.writeFileSync = () => { escribio = true; }; m.capturarSiCorresponde('test', {a:1}); fs.writeFileSync = orig; if (escribio) { console.error('FALLO: escribio a disco con CAPTURAR_GEOMETRIA sin activar'); process.exit(1); } console.log('OK: sin escritura con la variable apagada'); });"</automated>
  </verify>
  <done>capturaGeometria.js existe con el guard de variable de entorno verificado (cero escrituras cuando CAPTURAR_GEOMETRIA no es 'true'); arcgisRouting.js y osrmRouting.js llaman a capturarSiCorresponde en los 4 puntos descritos (crudo+normalizado x 2 proveedores) sin alterar ninguna otra función.</done>
  <acceptance_criteria>
    - `node --check` pasa sin errores de sintaxis en los 3 archivos tocados/creados.
    - El script de verificación automatizado confirma que `capturarSiCorresponde` no invoca `writeFileSync` cuando `CAPTURAR_GEOMETRIA` no vale `'true'`.
    - `git diff backend/src/utils/arcgisRouting.js` muestra únicamente el import nuevo y las dos llamadas a `capturarSiCorresponde` dentro de `normalizar()` -- ninguna otra línea de `solve()`, `obtenerToken()`, `elegirModo()` o `construirParadas()` cambia.
    - `git diff backend/src/utils/osrmRouting.js` muestra únicamente el import nuevo y las dos llamadas a `capturarSiCorresponde` dentro de `resolverRutaOsrm()` -- `redactarInstruccion()` y el resto del archivo quedan intactos.
    - Ningún archivo nuevo contiene coordenadas hardcodeadas ni ubicación real -- `capturaGeometria.js` solo recibe `contenido` como parámetro, no genera datos por sí mismo.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: captura dev-only del GeoJSON a MapLibre (etapa 4) + esqueleto de 04-COMPARACION-GEOMETRIA.md</name>
  <files>frontend/src/componentes/detalle/InteractiveMap.jsx, .planning/phases/04-fidelidad-de-curvas-y-rotondas/04-COMPARACION-GEOMETRIA.md</files>
  <read_first>
    frontend/src/componentes/detalle/InteractiveMap.jsx función `lineaGeoJSON(puntos)` (línea ~58) y el efecto de ajuste de bounds que ya itera `ruta.puntos` cuando `previsualizando` es true (línea ~319-331: `if (!mapListo || !previsualizando || !ruta?.puntos || ruta.puntos.length < 2) return; ... for (const [lat, lng] of ruta.puntos) { ... }`) -- este es el punto donde `ruta.puntos` (la geometría completa normalizada por el backend) ya está disponible antes de dividirse en `tramos.recorrido`/`tramos.restante`; 04-CONTEXT.md `<decisions>` D-02 (las 5 etapas) y `<code_context>` (mapa exacto de las 5 etapas a puntos de anclaje reales del código); `.planning/phases/01-diagn-stico-e-instrumentaci-n/01-DIAGNOSIS.md` si existe, como referencia de formato de informe de fase.
  </read_first>
  <behavior>
    - Dentro del mismo efecto que ya lee `ruta.puntos` en modo `previsualizando` (línea ~319), justo después de la comprobación de guard, si `import.meta.env.DEV` es true Y `window.__capturarGeometria === true` (flag manual que el humano activa desde la consola antes de pedir la ruta, nunca por defecto), hacer `console.log('[captura-geometria] geojson-maplibre', JSON.stringify(lineaGeoJSON(ruta.puntos)))` -- una sola vez por cambio de `ruta` (ya lo garantiza el array de dependencias del efecto `[mapListo, previsualizando, ruta]`).
    - Sin el flag `window.__capturarGeometria` activo, el comportamiento del efecto es idéntico al actual -- cero logs nuevos, cero coste adicional.
    - `04-COMPARACION-GEOMETRIA.md` skeleton incluye: encabezado de fase, sección "## Comparación de las 5 etapas" con una tabla vacía de columnas Etapa | Nº de vértices | Fuente/proveedor | Observaciones (5 filas: ArcGIS crudo, OSRM crudo, Normalizado backend, GeoJSON MapLibre, Resultado visual), sección "## Instrucciones de captura" con los pasos concretos, y sección "## Rotondas/curvas candidatas" con 1-2 sugerencias.
  </behavior>
  <action>
    En `InteractiveMap.jsx`, dentro del efecto de la línea ~319 (`useEffect(() => { if (!mapListo || !previsualizando || !ruta?.puntos || ruta.puntos.length < 2) return; ... }, [mapListo, previsualizando, ruta]);`), añadir justo después del guard de retorno temprano: `if (import.meta.env.DEV && window.__capturarGeometria === true) { console.log('[captura-geometria] geojson-maplibre', JSON.stringify(lineaGeoJSON(ruta.puntos))); }`. No modificar el resto del efecto (el cálculo de bounds sigue igual).
    Crear `.planning/phases/04-fidelidad-de-curvas-y-rotondas/04-COMPARACION-GEOMETRIA.md` con las secciones descritas en `<behavior>`. En "## Instrucciones de captura", escribir los pasos en orden: (1) elegir dos sitios turísticos ya existentes en la base de datos (`GET /api/sitios` o el panel admin) cuyo trazado por calles real de ArcGIS pase por una rotonda/curva pronunciada de Itagüí -- nunca usar la ubicación GPS real de una persona como origen; (2) levantar el backend con `CAPTURAR_GEOMETRIA=true node --watch src/index.js` desde `backend/` (o el equivalente en el gestor de procesos que uses) para activar la captura de las etapas 1-3; (3) levantar el frontend en modo desarrollo (`npm run dev` en `frontend/`) y, antes de pedir la ruta, abrir la consola del navegador y ejecutar `window.__capturarGeometria = true;` para activar la captura de la etapa 4; (4) en la página de detalle del sitio destino, iniciar la vista previa de ruta ("Fijar Ruta de Destino") entre los dos sitios elegidos en el paso 1; (5) revisar `backend/.planning/../.planning/phases/04-fidelidad-de-curvas-y-rotondas/captura/` (o directamente `.planning/phases/04-fidelidad-de-curvas-y-rotondas/captura/` desde la raíz del repo) -- deben aparecer 2 o 4 archivos JSON (arcgis-crudo + arcgis-normalizado, y también osrm-crudo + osrm-normalizado solo si el respaldo OSRM intervino); (6) en la consola del navegador, copiar la línea que empieza con `[captura-geometria] geojson-maplibre` y guardarla como `.planning/phases/04-fidelidad-de-curvas-y-rotondas/captura/<fecha>-geojson-maplibre.json`; (7) tomar una captura de pantalla del mapa mostrando la rotonda/curva renderizada y guardarla en la misma carpeta; (8) IMPORTANTE -- apagar la captura después: `window.__capturarGeometria = false;` en el navegador, y no dejar `CAPTURAR_GEOMETRIA=true` en ningún entorno de staging/producción persistente. En "## Rotondas/curvas candidatas", sugerir 1-2 ubicaciones conocidas de Itagüí (ej. una glorieta principal sobre una vía arteria) a validar por el dueño del milestone, dejando explícito que la elección final la confirma él en el Plan 04-02.
  </action>
  <verify>
    <automated>cd "C:/Users/MediaLab/orca/workspaces/Turismo-Arcgis/phase-4-geometria" && grep -q "__capturarGeometria" frontend/src/componentes/detalle/InteractiveMap.jsx && grep -q "^## Comparación de las 5 etapas" .planning/phases/04-fidelidad-de-curvas-y-rotondas/04-COMPARACION-GEOMETRIA.md && grep -q "^## Instrucciones de captura" .planning/phases/04-fidelidad-de-curvas-y-rotondas/04-COMPARACION-GEOMETRIA.md && grep -q "^## Rotondas/curvas candidatas" .planning/phases/04-fidelidad-de-curvas-y-rotondas/04-COMPARACION-GEOMETRIA.md</automated>
  </verify>
  <done>InteractiveMap.jsx registra el GeoJSON de la ruta completa a consola solo cuando DEV es true y window.__capturarGeometria vale true, sin cambiar el cálculo de bounds existente; 04-COMPARACION-GEOMETRIA.md tiene las 3 secciones del esqueleto (tabla vacía de 5 etapas, instrucciones de captura de 8 pasos, candidatas de rotonda).</done>
  <acceptance_criteria>
    - `grep -c "useEffect" frontend/src/componentes/detalle/InteractiveMap.jsx` no cambia respecto al valor previo al plan -- no se añadió ningún efecto nuevo, solo una línea dentro del existente.
    - `git diff frontend/src/componentes/detalle/InteractiveMap.jsx` muestra únicamente la captura dev-only añadida dentro del efecto de la línea ~319 -- ninguna otra línea del archivo cambia (en particular, `lineaGeoJSON`, las capas `ruta-recorrida-linea`/`ruta-restante-linea` y `enSeguimiento`/`mostrarTrayecto` quedan intactos, per D-06 de 04-CONTEXT.md que reserva cualquier cambio de representación visual hasta contar con evidencia del Plan 04-02).
    - `04-COMPARACION-GEOMETRIA.md` contiene una tabla markdown de 5 filas de datos (una por etapa) bajo "## Comparación de las 5 etapas", con las columnas numéricas y de fuente explícitamente vacías o marcadas "pendiente de captura".
    - Las instrucciones de captura mencionan explícitamente los pasos de activar (`CAPTURAR_GEOMETRIA=true`, `window.__capturarGeometria = true`) y desactivar la instrumentación al terminar, y prohíben usar la ubicación GPS real de una persona como origen.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Directorio de captura en disco (`.planning/phases/.../captura/`) | Superficie nueva de archivos JSON con geometría de ruta real, escrita solo cuando `CAPTURAR_GEOMETRIA=true` está presente explícitamente |
| Consola del navegador (`window.__capturarGeometria`) | Requiere que el humano active manualmente el flag antes de cada captura; nunca se activa solo |
| Respuesta HTTP normal de `/api/rutas/resolver` | No debe verse afectada por la instrumentación -- la captura es un efecto secundario de solo-lectura sobre datos que ya se iban a calcular |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Archivos JSON en `captura/` | medium | mitigate | Solo se escriben con `CAPTURAR_GEOMETRIA=true` explícito (nunca en producción/Railway, que no define esa variable); el contenido son coordenadas de una ruta de prueba entre sitios turísticos públicos, nunca ubicación GPS real de una persona (D-03 de 04-CONTEXT.md) |
| T-04-02 | Tampering (comportamiento de producción alterado por error) | `normalizar()` / `resolverRutaOsrm()` instrumentados | high | mitigate | Cada llamada a `capturarSiCorresponde` está envuelta en try/catch y comprueba la variable de entorno en cada invocación (nunca cacheada); un fallo de escritura nunca interrumpe ni altera el objeto normalizado que se retorna al llamador real |
| T-04-03 | Denial of Service (crecimiento de disco) | Directorio `captura/` durante una sesión de captura prolongada | low | accept | El volumen esperado es de unas pocas rutas de prueba capturadas manualmente por el dueño del milestone en una sesión corta, no tráfico de producción -- no se justifica límite automático de tamaño para esta instrumentación temporal |
</threat_model>

<verification>
`node --check` pasa en los 3 archivos backend tocados/creados; el guard de `CAPTURAR_GEOMETRIA` demuestra cero escrituras a disco cuando la variable está apagada; `InteractiveMap.jsx` solo gana una línea de captura dev-only dentro de un efecto existente; `04-COMPARACION-GEOMETRIA.md` existe con las 3 secciones del esqueleto completas y accionables.
</verification>

<success_criteria>
- El mecanismo de captura de las 4 primeras etapas (ArcGIS crudo, OSRM crudo, normalizado backend, GeoJSON MapLibre) existe y está verificablemente apagado por defecto.
- Ningún comportamiento de navegación en producción cambia -- solo instrumentación guardada por variable de entorno / flag de consola.
- `04-COMPARACION-GEOMETRIA.md` deja al Plan 04-02 instrucciones tan concretas que el dueño del milestone no tiene que inventar el procedimiento de captura.
</success_criteria>

<output>
Create `.planning/phases/04-fidelidad-de-curvas-y-rotondas/04-01-SUMMARY.md` when done
</output>