---
phase: 04-fidelidad-de-curvas-y-rotondas
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/phases/04-fidelidad-de-curvas-y-rotondas/captura/
autonomous: false
requirements: [GEOM-01]

estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "El dueño del milestone ejecutó la captura real contra al menos una rotonda/curva pronunciada de Itagüí, siguiendo las instrucciones ya automatizadas en 04-COMPARACION-GEOMETRIA.md (Plan 04-01) -- nada queda sin automatizar que Claude pudiera automatizar; solo la elección final de la rotonda y la ejecución física del flujo en el navegador quedan para el humano."
    - "Los artefactos reportados (archivos JSON de captura backend, GeoJSON de MapLibre pegado desde consola, captura de pantalla del resultado visual) existen y corresponden a la MISMA ruta de prueba entre dos sitios turísticos públicos -- no a una mezcla de sesiones distintas."
  artifacts:
    - .planning/phases/04-fidelidad-de-curvas-y-rotondas/captura/
  key_links:
    - "Instrucciones de captura de 04-COMPARACION-GEOMETRIA.md (Plan 04-01) -> ejecución física del humano (este plan) -> archivos crudos en captura/ -> síntesis en el mismo informe"
  prohibitions:
    - "No pedirle al humano nada que el agente pueda automatizar -- el mecanismo, las instrucciones y los criterios de síntesis ya están en el Plan 04-01; solo elegir la rotonda real, ejecutar el flujo y evaluar la captura quedan para el humano, porque un agente no puede decidir cuál rotonda representa el síntoma reportado (D-05 de 04-CONTEXT.md)."
    - "El reporte del humano no debe incluir la ubicación GPS real de una persona, recorridos completos ni tokens -- solo la geometría de la ruta de prueba entre los dos sitios turísticos elegidos (D-03 de 04-CONTEXT.md)."
  flagged_assumptions:
    - "GEOM-01 no tiene heurística en el edge-probe determinista del proyecto (mismo patrón que Fase 1 DIAG-01/02). La resolución manual de este plan es la traducción directa de D-01/D-05 de 04-CONTEXT.md (paso de ejecución humana marcado autonomous:false, mismo patrón que el Plan 01-05 de la Fase 1)."
---

<objective>
Pedirle al dueño del milestone que ejecute la captura real de las 5 etapas contra al menos una rotonda/curva pronunciada de Itagüí, usando el mecanismo y las instrucciones ya construidas en el Plan 04-01, y que reporte los artefactos resultantes (archivos backend, GeoJSON de consola, captura de pantalla).

Purpose: ningún agente puede recorrer una calle real de Itagüí ni decidir en campo cuál rotonda es representativa del síntoma reportado -- es la única pieza de la Fase 4 que exige una acción humana real, per D-01/D-05 de 04-CONTEXT.md ("Claude no puede verificar en campo cuál rotonda es representativa... el paso de ejecución humana confirma cuál se usó").
Output: contenido real en `.planning/phases/04-fidelidad-de-curvas-y-rotondas/captura/` (archivos JSON de las etapas 1-4 + captura de pantalla de la etapa 5), listo para completar la síntesis en `04-COMPARACION-GEOMETRIA.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-fidelidad-de-curvas-y-rotondas/04-CONTEXT.md
@.planning/phases/04-fidelidad-de-curvas-y-rotondas/04-COMPARACION-GEOMETRIA.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: el dueño del milestone ejecuta la captura real y reporta los artefactos</name>
  <precondition>El Plan 04-01 ya completó: el backend acepta `CAPTURAR_GEOMETRIA=true`, el frontend acepta `window.__capturarGeometria = true`, y `04-COMPARACION-GEOMETRIA.md` contiene instrucciones que bloquean el uso de GPS personal y exigen un origen manual público.</precondition>
  <action>Seguir los 9 pasos de "## Instrucciones de captura" en `04-COMPARACION-GEOMETRIA.md` contra al menos una rotonda/curva pronunciada real de Itagüí y reportar los artefactos resultantes.</action>
  <instructions>
    Ya automaticé todo lo previo: el backend y el frontend tienen el mecanismo de captura listo (Plan 04-01), y las instrucciones paso a paso ya están escritas en `.planning/phases/04-fidelidad-de-curvas-y-rotondas/04-COMPARACION-GEOMETRIA.md` sección "## Instrucciones de captura".

    Lo único que falta es que ejecutes esos 9 pasos contra una rotonda/curva real de Itagüí y me reportes aquí:
    1. Qué dos sitios turísticos usaste como origen/destino de la ruta de prueba, y qué rotonda/curva quedó en el trazado.
    2. La ruta o el listado de archivos que quedaron en `.planning/phases/04-fidelidad-de-curvas-y-rotondas/captura/` (deberían ser 2-5 archivos: arcgis-crudo, arcgis-normalizado, opcionalmente osrm-crudo/osrm-normalizado si el respaldo intervino, y el geojson-maplibre que pegaste desde la consola).
    3. Una descripción breve o la captura de pantalla del resultado visual (¿la rotonda se ve completa, sin cortar esquinas/separadores/edificios, siguiendo la calzada real?).

    No reportes la ubicación GPS real de una persona ni un recorrido completo -- solo la geometría de la ruta de prueba entre los dos sitios turísticos elegidos.
  </instructions>
  <verification>Existen archivos reales (no vacíos, con contenido JSON válido) en `.planning/phases/04-fidelidad-de-curvas-y-rotondas/captura/` correspondientes a la misma ruta de prueba, y el humano confirmó cuál rotonda/curva se usó y compartió su observación (o captura de pantalla) del resultado visual.</verification>
  <resume-signal>Pega aquí la lista de archivos generados en captura/ (o su contenido si son pequeños), qué rotonda/curva usaste, y tu observación del resultado visual, para continuar.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Reporte humano pegado en la conversación de ejecución | Los datos cruzan de un canal no controlado (copy/paste humano) hacia artefactos versionados del proyecto |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Information Disclosure | Reporte humano de la captura | medium | mitigate | Las instrucciones (heredadas del Plan 04-01) piden explícitamente solo la geometría de la ruta de prueba entre sitios públicos, nunca ubicación GPS real de una persona ni recorrido completo; este plan repite la misma restricción en `<instructions>` |
</threat_model>

<verification>
`.planning/phases/04-fidelidad-de-curvas-y-rotondas/captura/` contiene archivos reales producidos por una sesión de captura contra una rotonda/curva real de Itagüí, y el dueño del milestone confirmó cuál ubicación se usó y su observación del resultado visual.
</verification>

<success_criteria>
- Existe evidencia real (no simulada) de las etapas 1-4 en `captura/`, más una observación o captura de pantalla de la etapa 5 (resultado visual).
- El dueño del milestone confirmó explícitamente cuál rotonda/curva de Itagüí se usó.
- Ningún dato reportado incluye ubicación GPS real de una persona, recorrido completo o tokens.
</success_criteria>

<output>
Create `.planning/phases/04-fidelidad-de-curvas-y-rotondas/04-02-SUMMARY.md` when done
</output>
